@* @using Ventoura.Application.ViewModels.Tours;
@model ICollection<TourItemVM>
@{
    int count = 0;
}
<style>
    .resized-image {
        max-width: 60px; /* Maksimum genişlik */
        max-height: 55px; /* Maksimum yükseklik */
    }
   
</style>

<div class="page-body">
    <div class="container-fluid">
        <div class="page-title">
            <div class="row">
                <div class="col-6">
                    <h4>Tour Table</h4>
                </div>
                <div class="col-6"> 
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid starts-->
    <div class="container-fluid basic_table">
        <div class="row">
            <div class="col-sm-12">
                <div class="card">  
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr class="border-bottom-primary">
                                    <th scope="col">No</th>
                                    <th scope="col">Tour Name</th>
                                    <th scope="col">Start Time</th>
                                    <th scope="col">Country</th>
                                    <th scope="col">City</th>
                                    <th scope="col">Price</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr class="border-bottom-secondary">
                                        <th scope="row">@count</th>
                                        <td><img class="img-30 me-2" src="~/rev-slider-files/assets/@item.TourImages.FirstOrDefault(c=>c.IsPrimary==true).Url">@item.Name</td>
                                        <td>@item.StartDate</td>
                                        <td>@item.Country.Name</td>
                                        <td>@item.City.Name</td>
                                        <td>@item.Price</td>
                                        <td>
                                            <a asp-action="Update" asp-area="VentouraAdmin" asp-controller="Tour">
                                                <img src="~/main/img/system-update.png" class="resized-image" alt="Contract Image">
                                            </a>
                                        </td>
                                        <td>
                                            <a asp-action="Update" asp-area="VentouraAdmin" asp-controller="Tour">
                                                <img src="~/main/img/icons8-cancel.gif" class="resized-image" alt="Contract Image">
                                            </a>
                                        </td>
                                        <td>
                                            <button type="button" class="btn btn-outline-info">Details</button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->
</div>
 *@

@using Ventoura.Application.ViewModels.Tours;
@model ICollection<TourItemVM>
@{
    int count = 0;
}
    <style>
        .resized-image {
            max-width: 60px; /* Maksimum genişlik */
            max-height: 55px; /* Maksimum yükseklik */
        }
        .animate {
            animation: spin 1s infinite;
        }
        .keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
    </style>
    <div class="page-body">
        <div class="container-fluid">
            <div class="page-title">
                <div class="row">
                    <div class="col-6">
                        <h4>Tour Table</h4>
                    </div>
                    <div class="col-6"> 
                    </div>
                </div>
            </div>
        </div>
        <!-- Container-fluid starts-->
        <div class="container-fluid basic_table">
            <div class="row">
                <div class="col-sm-12">
                    <div class="card">  
                        <div class="table-responsive">
                            <table class="table">
                                <thead>
                                    <tr class="border-bottom-primary">
                                        <th scope="col">No</th>
                                        <th scope="col">Tour Name</th>
                                        <th scope="col">Start Time</th>
                                        <th scope="col">Country</th>
                                        <th scope="col">City</th>
                                        <th scope="col">Price</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr class="border-bottom-secondary">
                                            <th scope="row">@count</th>
                                            <td>
                                                <img class="img-30 me-2 resized-image" src="~/rev-slider-files/assets/@item.TourImages.FirstOrDefault(c=>c.IsPrimary==true).Url">@item.Name
                                            </td>
                                            <td>@item.StartDate</td>
                                            <td>@item.Country.Name</td>
                                            <td>@item.City.Name</td>
                                            <td>@item.Price</td>
                                            <td>
                                                <a asp-action="Update" asp-area="VentouraAdmin" asp-controller="Tour">
                                                    <img src="~/main/img/system-update.png" class="resized-image" alt="Contract Image">
                                                </a>
                                            </td>
                                            <td>
                                                <a asp-action="Update" asp-area="VentouraAdmin" asp-controller="Tour">
                                                    <img src="~/main/img/icons8-cancel.gif" class="resized-image animate" alt="Contract Image">
                                                </a>
                                            </td>
                                            <td>
                                                <button type="button" class="btn btn-outline-info">Details</button>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- Container-fluid Ends-->
    </div>
    <!-- JavaScript -->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const gifs = document.querySelectorAll('.resized-image');

            gifs.forEach(gif => {
                gif.addEventListener('mouseenter', () => {
                    gif.classList.add('animate');
                });

                gif.addEventListener('mouseleave', () => {
                    gif.classList.remove('animate');
                });
            });
        });
    </script>
